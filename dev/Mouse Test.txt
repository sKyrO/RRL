program RRLMouseTest;
{.include RRL\RRL.txt}

const
  Moves = 5;

var
  w, h, i: Integer;
begin
  SetupRRL;
  GetClientDimensions(w, h);
  for i := 1 to Moves do
    r_MoveMouse(Random(w), Random(h), 0, 0);
end.